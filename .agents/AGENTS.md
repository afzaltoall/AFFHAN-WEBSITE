
- Never commit or push to GitHub unless the user explicitly says 'deploy to github' and 'make website live to vercel'.
- NEVER commit auto-generated configuration files (e.g. `vercel.json` generated by CLI) without explicit user review. Always carefully review `git status` to ensure you do not push broken routing or environment configs to the live site.
